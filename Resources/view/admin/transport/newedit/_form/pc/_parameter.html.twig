{% trans_default_domain 'admin.delivery.auto' %}

<div class="d-flex justify-content-between gap-3">

    {# Длина (Глубина), см #}
    {{ form_row(form.parameter.length, { label: 'admin.form.label.length'|trans } ) }}

    {# Ширина, см #}
    {{ form_row(form.parameter.width, { label: 'admin.form.label.width'|trans } ) }}

    {# Высота, см #}
    {{ form_row(form.parameter.height, { label: 'admin.form.label.height'|trans } ) }}

</div>


